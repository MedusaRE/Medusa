image: debian

before_script:
    - git submodule init
    - git submodule update
    - tools/deps_debian.sh

all:
    script:
        - echo "Hello, world!"
        - clang++
    artifacts:
        untracked: false
        expire_in: "30 days"